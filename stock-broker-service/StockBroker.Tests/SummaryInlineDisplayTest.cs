using NSubstitute;
using NUnit.Framework;

namespace StockBroker.Tests
{
    public class SummaryInlineDisplayTest
    {
        //[Test]
        //public void print_no_orders_summary()
        //{
        //    var printer = Substitute.For<Printer>();
        //    Display display = new SummaryInlineDisplay(printer);
        //    var orderDate = new DateTime(2000, 1, 1, 12, 0, 0);

        //    display.Show(new Summary(orderDate, 0, 0));

        //    printer.Received(1).Print($"{orderDate} Buy: € 0.00, Sell: € 0.00");
        //}
    }
}
